echo "Starting deployment script run" && sudo yum update && curl -sL https://rpm.nodesource.com/setup | sudo bash - && sudo yum install -y nodejs && sudo ln -sf /usr/bin/nodejs /usr/bin/node && sudo npm install && sudo yum install -y nginx && sudo rm -f /etc/nginx/sites-enabled/default && sudo cp frontend-app-nginx-config /etc/nginx/sites-available/ && sudo ln -sf /etc/nginx/sites-available/frontend-app-nginx-config /etc/nginx/sites-enabled/frontend-app-nginx-config && sudo service nginx restart && sudo npm install -g pm2 && sudo pm2 start -f app.js && sudo pm2 startup systemd && sudo pm2 save && echo "End of script run"